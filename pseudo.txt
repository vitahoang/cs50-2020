declare max_voter, max_candidates
declare preference table
struct candidate 
    name
    voters
    emiminated
declare voter_count; candidate_count
struct an candidates[max_candidates]
get candidate name then put it to the struct candidate
get the number of voters then put it to voter voter_count
get vote list of each voter
    for i < voter_count:
        for j < candidate_count
            string name = get_string ("Rank i%, j + 1)
            

